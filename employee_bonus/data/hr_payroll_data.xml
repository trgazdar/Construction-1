<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!--Salary rule for bonus -->
       <!-- <record id="hr_salary_rule_bonus_amounts" model="hr.salary.rule">
            <field name="code">Bonus</field>
            <field name="name">Bonus</field>
            <field name="category_id" ref="hr_payroll.ALW"/>
            <field name="amount_select">code</field>
            <field name="amount_python_compute">result = employee.get_bonus_amounts(employee.id, payslip.date_from, payslip.date_to)</field>
            <field name="sequence" eval="11"/>
        </record>
        	-->

    </data>
</odoo>